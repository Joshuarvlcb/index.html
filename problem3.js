let radius= prompt("What is the radius of the cylinder");
let height =prompt("What is the height of the cyclinder");

let area =Math.PI * radius**2;
let volume = Math.round(area * height * 12);

console.log(" The volume of a cyclinder is " + volume + "inches cubed")  
console.log(`the volume of the cylinder is ${volume}`)